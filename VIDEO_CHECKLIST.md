# Final Video Checklist

Do not submit until every box can be checked.

- [ ] Face and student ID are clear at the beginning.
- [ ] Name `Garrah Thabit` and matric `A24CS4013` are stated.
- [ ] Video duration is at least five minutes.
- [ ] Production MySQL service is visibly running.
- [ ] Database name, tables and sample record are shown.
- [ ] Slim API public URL is visible.
- [ ] `GET /api/books` returns status 200 and JSON.
- [ ] `POST /api/books` returns status 201 and JSON.
- [ ] The POST-created row is shown in MySQL.
- [ ] `npm run build` and the `dist` folder are shown.
- [ ] Vue public URL is visible.
- [ ] Vue displays records loaded from the API.
- [ ] `package.json` shows Capacitor dependencies.
- [ ] `capacitor.config.json` shows app name, app ID and `webDir: dist`.
- [ ] Android emulator or physical device launches the app.
- [ ] Android app loads or creates data through the production API.
- [ ] No `.env`, JWT secret or database password appears in the recording.
- [ ] Final video plays correctly after export.
